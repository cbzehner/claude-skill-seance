interface:
  display_name: "Seance"
  short_description: "Search past local agent sessions and resume work"
  default_prompt: "Use $seance to find the relevant past session and reconstruct what happened."
